// Warehouse helpers placeholder
export const calculateStockValue = (items: any[]) => 0;
export const validateStockMovement = (movement: any) => true;

export default { calculateStockValue, validateStockMovement };