// POS helpers placeholder
export const calculateDiscount = (amount: number, rate: number) => amount * rate;
export const formatReceipt = (sale: any) => '';

export default { calculateDiscount, formatReceipt };