// Settings helpers placeholder
export const validateSettings = (settings: any) => true;
export const saveSettings = (settings: any) => Promise.resolve();

export default { validateSettings, saveSettings };